# Supabase Configuration
# Get these from: Supabase Dashboard → Settings → API
EXPO_PUBLIC_SUPABASE_URL=your-project-url
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# OpenAI API Key (Optional - for Healthy Picks recommendations)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here
# OR use EXPO_PUBLIC prefix for client-side access (less secure)
# EXPO_PUBLIC_OPENAI_API_KEY=your-openai-api-key-here

# Supabase Service Role Key (ONLY for seeding scripts - NEVER commit this!)
# Get from: Supabase Dashboard → Settings → API → service_role key
# This key bypasses RLS and has admin privileges - keep it secret!
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# API URL (Optional - for recommendations endpoint)
# If deploying separately, set this to your API server URL
# EXPO_PUBLIC_API_URL=http://localhost:3000
